def are_you_playing_banjo(name):
    # Conditional if first index of name is equal to "r" elif "R" else
    if (name[0] == "r"):
        return name + " plays banjo"
    elif (name[0] == "R"):
        return name + " plays banjo"
    else:
        return name + " does not play banjo"